import './style.scss'

const main = document.querySelector("main") as HTMLElement;

const mapIcon = document.getElementById("map-icon") as HTMLImageElement;

mapIcon.addEventListener("click", addMap);

// TODO lägg till DOM för karta
